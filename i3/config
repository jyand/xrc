for_window [class="^.*"] border pixel 4
gaps inner 8
gaps outer 0
set $t $TERMINAL
set $m Mod4

# Set wallpaper
exec feh --bg-fill ~/scripts/bkgnd.jpg

# Set Xresources colors:
set_from_resource $darkblack	color0  #000000
set_from_resource $black	color8  #000000
set_from_resource $darkred	color1  #000000
set_from_resource $red		color9  #000000
set_from_resource $darkgreen	color2  #000000
set_from_resource $green	color10 #000000
set_from_resource $darkyellow	color3  #000000
set_from_resource $yellow	color11 #000000
set_from_resource $darkblue	color4  #000000
set_from_resource $blue		color12 #000000
set_from_resource $darkmagenta	color5  #000000
set_from_resource $magenta	color13 #000000
set_from_resource $darkcyan	color6  #000000
set_from_resource $cyan		color14 #000000
set_from_resource $darkwhite	color7  #000000
set_from_resource $white	color15 #000000

#                       BORDER		BACKGROUND	TEXT		INDICATOR   CHILD_BORDER
client.focused		$red		$red		$magenta	$darkmagenta		$darkblue
client.unfocused	$transparent	$blue		$white		$darkblue		$darkblack
client.focused_inactive	$transparent	$blue		$white		$darkblue		$darkblack
client.urgent		$darkred	$darkred	$black		$darkred		$darkred
client.background $black

bar {
	font pango:mono 8
	colors {
			background $darkblack
			statusline $white
			separator $red
			focused_workspace $cyan $darkwhite $darkmagenta
      			inactive_workspace $magenta $darkmagenta $black
        }
	status_command i3blocks
	position top
	mode dock
	modifier None
}

bindsym $m+q			kill

bindsym $m+Return               exec $t $FILE
bindsym $m+Escape 		exec st -e fish
bindsym $m+plus                 gaps outer current minus 8
bindsym $m+slash                gaps outer current plus 8
bindsym $m+equal                gaps inner current minus 2
bindsym $m+minus                gaps inner current plus 2
bindsym $m+space		split toggle

bindsym $m+y                    exec dispsel
bindsym $m+t                    exec $t -e nmtui

bindsym $m+u  		        exec winresize left
bindsym $m+i		        exec winresize down
bindsym $m+o 			exec winresize up
bindsym $m+p			exec winresize right

bindsym $m+r                    exec redshift -O 2500
bindsym $m+e                    exec redshift -x

bindsym $m+d			exec dmenu_run
bindsym $m+s            	exec maim -s ~/img/screenshots/$(date +%s).png

bindsym $m+f			fullscreen toggle
bindsym $m+g                    bar mode toggle
bindsym $m+a			floating toggle

bindsym $m+h			focus left
bindsym $m+period               move left 30
bindsym $m+j			focus down
bindsym $m+underscore           move down 30
bindsym $m+k			focus up
bindsym $m+asterisk		move up 30
bindsym $m+l			focus right
bindsym $m+semicolon	        move right 30

bindsym $m+v 		        exec torbrowser-launcher
bindsym $m+b                    exec $BROWSER ~/.homepage

bindsym $m+c                    exec pamixer -i 5
bindsym $m+x                    exec pamixer -d 5
bindsym $m+z                    exec pamixer -t

bindsym $m+asciitilde           exec $t -e gotop -c vice -pafs
bindsym $m+Tab                  exec $t -e htop -t --sort-key PERCENT_CPU --sort-key USER --sort-key PERCENT_MEM

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"

# switch to workspace
bindsym $m+w            workspace back_and_forth
bindsym $m+n  	        workspace $ws1
bindsym $m+m  	        workspace $ws2
bindsym $m+comma  	workspace $ws3
bindsym $m+colon  	workspace $ws4

# move focused container to workspace
bindsym $m+1            move container to workspace $ws1
bindsym $m+2 	        move container to workspace $ws2
bindsym $m+3            move container to workspace $ws3
bindsym $m+4            move container to workspace $ws4

for_window [class="Zathura"] floating enable
for_window [class="mpv"] floating enable
for_window [class="sxiv"] floating enable
for_window [class="feh"] floating enable
for_window [class="png"] floating enable
for_window [class="jpg"] floating enable
for_window [class="bmp"] floating enable
for_window [class="gif"] floating enable
